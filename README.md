# Lions Club Rapport

Next.js + Tailwind CSS app for generating and previewing Lions Club meeting reports.

## Setup

1. Install dependencies: `npm install`
2. Run the development server: `npm run dev`
3. Open `http://localhost:3000`

## Features

- Meeting form with Tailwind UI
- Live report preview
- JPG export using `html2canvas`
- Reset form to defaults
