This repository contains a Next.js project scaffolded for a Lions Club meeting report generator.

- Use `npm install` to install dependencies.
- Use `npm run dev` to start the local development server.
- The app uses Tailwind CSS and html2canvas for generating JPG export from the report preview.
